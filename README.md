# hikkaku

## Setup

```bash
bun install
```

## Development example

```bash
bun run dev
```

This workspace contains the `hikkaku` package in `packages/hikkaku` and the dev example in
`examples/example1`.
