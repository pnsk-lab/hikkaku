{
  "name": "reversi",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development vite",
    "typecheck": "tsgo",
    "build": "bun run example:build",
    "train:nn": "python3 tools/train_reversi_nn.py --games 4000 --output-json src/nn-weights.json --output-ts src/nnWeights.ts",
    "example:build": "vite build"
  },
  "dependencies": {
    "hikkaku": "workspace:*"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260127.1",
    "vite": "8.0.0-beta.8"
  }
}
