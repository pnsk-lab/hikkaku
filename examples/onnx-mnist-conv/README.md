# ONNX MNIST Conv Example

Small MNIST convolution example for `@hikkaku/onnx`.

## Commands

```bash
bun run model:refresh
bun run dev
```

- `C`: clear the canvas
- `S`: load the next stored MNIST sample
- mouse drag: draw and re-run inference
